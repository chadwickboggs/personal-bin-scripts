#!/usr/bin/env bash

grep_excludes_file="~/stuff/grep_excludes_for_lspf.txt"

if [[ -f "${grep_excludes_file}" ]]; then
	lsp "find $@" | grep -vf "${grep_excludes_file}" | sort -ud
else
	lsp "find $@" | sort -ud
fi

exit $?
